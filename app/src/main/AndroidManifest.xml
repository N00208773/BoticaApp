<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BoticaApp"
        tools:targetApi="31">

        <!-- SplashActivity como lanzadora -->
        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Login y Register (sin intent-filter) -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false"/>
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false"/>

        <!-- Actividades por rol -->
        <activity
            android:name=".ui.cliente.ClienteActivity"
            android:exported="false"/>
        <activity
            android:name=".ui.empleado.EmpleadoActivity"
            android:exported="false"/>
        <activity
            android:name=".ui.admin.AdminActivity"
            android:exported="false"/>

        <!-- MainActivity ya no es lanzadora; puedes eliminarla o dejarla sin intent-filter -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            tools:node="remove"/>
    </application>
</manifest>
